<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Studio" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Studio" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
    <script>

    jQuery(function(){
      jQuery(".player").mb_YTPlayer();
    });

  </script>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <div id="pippo" style="position:relative; top:0;left:0;z-index:10">
              <a class="player" data-property="{videoURL:'https://www.youtube.com/watch?v=J4fP9D0or7M',containment:'body',autoPlay:true, mute:false, startAt:0,opacity:1}">My video</a> 
              <%= yield %>
            </div>
         
  </body>
</html>
